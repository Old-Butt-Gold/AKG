<Window x:Class="AKG.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AKG.UI"
        xmlns:viewModels="clr-namespace:AKG.UI.MVVM.ViewModels"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:objects="clr-namespace:AKG.Core.Objects;assembly=AKG.Core"
        mc:Ignorable="d"
        Title="3D Obj Viewer" Loaded="MainWindow_OnLoaded" WindowState="Maximized"
        FocusManager.FocusedElement="{Binding ElementName=ImgDisplay}">
    <Window.DataContext>
        <viewModels:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate x:Key="ModelInfoTemplate">
            <Border Background="LightGray" Padding="5">
                <TextBlock Text="{Binding SelectedModelInfo}" TextWrapping="Wrap"/>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Name="StackPanel" Orientation="Horizontal" Background="LightGray">
            <Button Focusable="False" x:Name="BtnLoad" Content="Load .obj" Margin="5" Padding="5" Command="{Binding LoadFileCommand}"/>
            <Button Focusable="False" x:Name="BtnClear" Content="Clear Screen" Margin="5" Padding="5" Command="{Binding ClearSceneCommand}"/>
            <Button Focusable="False" Content="Edit Camera" Margin="5" Padding="5" Command="{Binding EditCameraCommand}"/>
            <TextBlock Text="{Binding Fps, StringFormat=FPS: {0:F1}}" VerticalAlignment="Center" Margin="15,0,5,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Foreground Color:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Focusable="False" Content="Pick Color" Command="{Binding PickForegroundColorCommand}" Width="100" Height="30" Margin="0 0 5 0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Background Color:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Focusable="False" Content="Pick Color" Command="{Binding PickBackgroundColorCommand}" Width="100" Height="30"/>
            </StackPanel>
            <Button Focusable="False" Content="Model Info" ClickMode="Press" Command="{Binding ToggleModelInfoCommand}" Margin="10" Padding="5"/>
            
            <Popup x:Name="ModelInfoPopup" Placement="Bottom" PlacementTarget="{Binding ElementName=StackPanel}" IsOpen="{Binding IsModelInfoVisible}" StaysOpen="False">
                <ContentControl Content="{Binding}" ContentTemplate="{StaticResource ModelInfoTemplate}"/>
            </Popup>
            <ComboBox HorizontalAlignment="Center" VerticalAlignment="Center" Focusable="False" SelectedValue="{Binding SelectedRenderMode}" SelectedValuePath="Tag" Margin="5">
                <ComboBoxItem Content="Wireframe" Tag="{x:Static objects:RenderMode.Wireframe}"/>
                <ComboBoxItem Content="Filled Triangles Lambert" Tag="{x:Static objects:RenderMode.FilledTrianglesLambert}"/>
                <ComboBoxItem Content="Filled Triangles Phong" Tag="{x:Static objects:RenderMode.FilledTrianglesPhong}"/>
                <ComboBoxItem Content="Filled Triangles Average Face Normal Phong" Tag="{x:Static objects:RenderMode.FilledTrianglesAverageFaceNormalPhong}"/>
                <ComboBoxItem Content="Texture" Tag="{x:Static objects:RenderMode.Texture}"/>
            </ComboBox>

        </StackPanel>
        
        <StackPanel Grid.Row="1" Name="ImagePanel" Background="LightGray" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            
            <Image Focusable="True" Name="ImgDisplay" SnapsToDevicePixels="True"
                   Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"
                   Source="{Binding WriteableBitmap}" Cursor="Hand">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseWheel">
                        <i:InvokeCommandAction Command="{Binding MouseWheelCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseMove">
                        <i:InvokeCommandAction Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding MouseLeftButtonDownCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="MouseRightButtonDown">
                        <i:InvokeCommandAction Command="{Binding MouseRightButtonDownCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="KeyDown">
                        <i:InvokeCommandAction Command="{Binding KeyDownCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Image>
        </StackPanel>
    </Grid>
</Window>